// settings_tabs fixture unit 0015
#include <string>
#include <vector>

namespace fixture15 {

int Compute01(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 2 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute02(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 3 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute03(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 4 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute04(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 5 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute05(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 6 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute06(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 7 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute07(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 8 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute08(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 9 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute09(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 10 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute10(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 11 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute11(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 12 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute12(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 13 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute13(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 14 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute14(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 15 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute15(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 16 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute16(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 17 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute17(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 18 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute18(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 19 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute19(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 20 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

int Compute20(const std::vector<int>& values) {
  int total = 0;
  for (int value : values) {
    if (value % 21 == 0) {
      total += value;
    } else {
      total -= value / 2;
    }
  }
  return total;
}

}  // namespace fixture15
