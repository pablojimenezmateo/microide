---
name: Bug report
about: Something behaves incorrectly
labels: bug
---

**What happened, and what did you expect instead?**

**Steps to reproduce**
1.
2.

**Environment**
- microide version (`microide --version`):
- Installed via: `.deb` / built from source / Nix
- Distro and version:
- Display server: X11 / Wayland  *(only if it is a rendering, input, or cursor issue)*

**Anything else**
Logs, a screenshot, or the output of the command you ran. If a test or build failed,
`tools/run-checks.sh` writes the full output to `/tmp/microide-<target>.log` — that
file is far more useful than a summary.
